{
	"name": "final",
	"version": "1.0.0",
	"description": "",
	"main": "",
	"scripts": {
		"docker:start": "docker compose -f compose.yaml --env-file .env up --build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/PaulMares/crud-react-node-mySQL-go.git"
	},
	"private": true
}
